<p>Select Layers</p>
<form action="" id="mapLayerCtrl">
    <% for(var i =0; i < layers.length; i++) { %>
    <% var layer = layers[i]; %>
    <input type="radio" name=city value=<%= layer %> > <%= layer %> <br>
    <% } %>
</form>
<style>
    #ctrl-container {
        position: fixed;
        top: 55px;
        right: 0;
        bottom: 0;
        background: #F4F4F4;
        width: 20%;
        box-sizing: border-box;
        box-sizing: -moz-border-box;
        padding: 20px;
    }
</style>