/**
 * Created by Nathan P on 3/23/14.
 */

define(['jquery',
    'underscore',
    'backbone',
    'tinycolor',
    'leafletDraw',
    'text!MapViewTemplate.ejs'
], function ($, _, Backbone) {

    var CtrlSelectorView = new Backbone.View.extend({

    });

    return CtrlSelectorView;
});